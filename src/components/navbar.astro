---
---
<section>
    <div class="sticky top-5 w-64 bg-second text-white m-6 p-6 shadow-xl/30 rounded-xl">
    <div class="flex justify-center mb-6">
    <div class="flex-shrink-0 w-10 h-10 sm:w-30 sm:h-30 md:w-42 md:h-42 lg:w-50 lg:h-50 rounded-full bg-accent shadow-lg mb-6 md:mb-0">
    </div>
    </div>
    <div class="block text-center mb-12">
        <p>Name Surname</p>
        <p>Description</p>
    </div>
    <nav class="flex flex-col space-y-4">
                <a id="top" href="#top" 
   class="relative inline-block bg-second text-white font-bold py-2 px-4 rounded 
          hover:bg-button hover:text-accent transition-colors duration-300
          border-b-4 border-b-transparent
          overflow-hidden
          custom-underline">
  Top
        </a>
        <a id="about" href="#about" 
   class="relative inline-block bg-second text-white font-bold py-2 px-4 rounded 
          hover:bg-button hover:text-accent transition-colors duration-300
          border-b-4 border-b-transparent
          overflow-hidden
          custom-underline">
  About
        </a>
                <a id="projects" href="#projects" 
   class="relative inline-block bg-second text-white font-bold py-2 px-4 rounded 
          hover:bg-button hover:text-accent transition-colors duration-300
          border-b-4 border-b-transparent
          overflow-hidden
          custom-underline">
  Side Projects
</a>
        <a id="contact" href="#contact" 
   class="relative inline-block bg-second text-white font-bold py-2 px-4 rounded 
          hover:bg-button hover:text-accent transition-colors duration-300
          border-b-4 border-b-transparent
          overflow-hidden
          custom-underline">
  Contact
</a>
  </nav>
</div>
</section>
<script>
    document.addEventListener('DOMContentLoaded', () => {
  const sections = document.querySelectorAll('section[id]');
  const navLinks = document.querySelectorAll('nav a');

  function onScroll() {
    const scrollPos = window.scrollY + 100;

    sections.forEach(section => {
      const top = section.offsetTop;
      const bottom = top + section.offsetHeight;
      const id = section.getAttribute('id');

        if (scrollPos >= top && scrollPos < bottom) {
        navLinks.forEach(link => {
          link.classList.toggle('active', link.getAttribute('href') === `#${id}`);
        });
      }
    });
  }

  window.addEventListener('scroll', onScroll);
  onScroll();
});

</script>
